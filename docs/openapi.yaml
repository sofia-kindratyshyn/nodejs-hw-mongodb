openapi: 3.1.0
info:
  version: 1.0.0
  title: Contacts-App
  license:
    name: Apache 2.0
    url: http://www.apache.org/licenses/LICENSE-2.0.html
  description: App that provides storage and interact with contacts
tags:
  - name: Contacts
  description: Operations about contacts.
servers:
  - url: http://localhost:3000
  - url: #посилання на задеплоєний сайт
paths:
  /students/{id}:
    get:
      $ref: ../swagger/paths/contacts/{id}/get.yaml
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
